[project]
name = "voice-mcp"
version = "0.1.0"
description = "A bare bones mcp for a chatbot to talk agentically"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
]
